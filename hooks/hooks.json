{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "tool == 'Edit' && tool_input.file_path matches '\\.(ts|tsx|js|jsx)$'",
        "hooks": [
          {
            "type": "command",
            "command": "#!/bin/bash\nif grep -n 'console\\.log' \"$CLAUDE_TOOL_INPUT_FILE_PATH\" 2>/dev/null; then echo '[Hook] Remember to remove console.log before committing' >&2; fi"
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "stop_reason == 'end_turn'",
        "hooks": [
          {
            "type": "command",
            "command": "#!/bin/bash\necho '[Reminder] Run tests and typecheck before pushing'"
          }
        ]
      }
    ]
  }
}
